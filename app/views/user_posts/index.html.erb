<% content_for :title, "#{@user.display_name}さんの投稿" %>
<h1><%= @user.display_name %>さんの投稿</h1>

<p class="back-link"><%= link_to "参加者一覧に戻る", users_path %></p>

  <% if @posts.any? %>
    <%= render @posts %>
  <% else %>
    <p>まだ投稿がありません。</p>
  <% end %>
