<% if user_signed_in? %>
  <%= turbo_frame_tag "like_post_#{post.id}" do %>
    <% like = current_user.likes.find_by(post: post) %>

    <div class="like-row" role="status" aria-live="polite">
      <% if like %>
        <%= button_to "♥ #{post.likes.count}",
              post_like_path(post, like),
              method: :delete,
              class: "like-action-button liked",
              title: "いいねを取り消す" %>
      <% else %>
        <%= button_to "♡ #{post.likes.count}",
              post_likes_path(post),
              method: :post,
              class: "like-action-button",
              title: "いいね" %>
      <% end %>
    </div>
  <% end %>
<% end %>
